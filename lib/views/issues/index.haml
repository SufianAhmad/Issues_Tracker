%h1 All Issues

%table.table.table-hover
  %thead
    %tr
      %th Title
      %th ID
      %th Description
      %th Created at
  -@issues.each do |issue|
    %tr
      %td
        %a(href="/issues/#{issue.id}/edit")=issue.name
      %td=issue.id
      %td=issue.description
      %td=issue.id.generation_time.ago_in_words
  -unless @issues.any?
    %tfoot
      %tr
        %th
          There is no issue
          %a.btn.btn-primery(href="/issues/new") Create New    
